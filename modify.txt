Page
# remove list/table add id="content"
page list -> lists
dialogs -> forms
add buttons to form copy from transact.html

modules
# remove tooltip initialization
# remove $apply at validat method
# modify calling method
  remove dialog
# add self.save
  copy form onOk

# modify self.list
  add ajax at bottom
			$('#content').load('forms/[form].html',function() {
				$timeout(function() { $compile($('#content')[0])(scope); }, 500);
			});	

# call self.list at bottom of self.data
self.list(scope);